<h1 class="text-center mt-4"> Category : <%= @category.title %></h1>
  <div class= 'text-center mt-4'>
    <%= link_to "Edit category", edit_category_path(@category), class: 'btn btn-outline-info'%>


<% if @items == 0%>
  <h3 class="text-center mt-4">Recipes</h3>
<% end %>
 
<%= render 'items/item' %>